// const getObj = (time, correct) => {
//   const obj = {};
//   obj.isCorrect = correct || false;
//   obj.time = time || Math.round(Math.random() * 30);
//   return obj;
// };

// const getArray = (arrayLength, time, correct) => {
//   const array = [];
//   for (let i = 0; i < arrayLength; i++) {
//     array.push(getObj(time, correct));
//   }
//   return array;
// };
